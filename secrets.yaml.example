# ESPHome Secrets Template
#
# Copy this file to 'secrets.yaml' and fill in your actual values
# IMPORTANT: Never commit secrets.yaml to git!
#
# Generate encryption key with:
#   esphome random-key
#
# Or use Python:
#   python3 -c 'import secrets; print(secrets.token_hex(32))'

# API Encryption Key (required for secure Home Assistant connection)
# Generate with: esphome random-key
api_encryption_key: "REPLACE_WITH_YOUR_32_BYTE_BASE64_KEY"

# OTA Password (required for secure over-the-air updates)
# Use a strong password (16+ characters)
esphome_ota_password: "REPLACE_WITH_STRONG_PASSWORD"

# Optional: WiFi credentials (if using WiFi instead of Ethernet)
# wifi_ssid: "YourNetworkName"
# wifi_password: "YourWiFiPassword"
